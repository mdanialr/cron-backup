log_dir: "/full/path/to/dir"
backup:
  root_dir: "/full/path/to/dir"
  days_number_to_retain: 6
  database:
    # this database backup uses:
    # command: pg_dump
    # user: postgres
    # params: --clean --no-owner
    days_number_to_retain:
    databases:
      - database:
          dbname: db_name
          backup_dir_name: db-backupname
      - database:
          dbname: db_name
          backup_dir_name: db-backupname
  app:
    days_number_to_retain:
    apps:
      - appfile:
          app_dir: "/full/path/to/app/dir"
          backup_dir_name: someappname
      - appfile:
          app_dir: "/full/path/to/another/app/dir"
          backup_dir_name: someanotherappname